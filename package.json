{
  "name": "vize-deploy-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main",
  "scripts": {
    "start:dev": "nest start --watch",
    "start:dev:local": "./node_modules/@vize/cgi/node_modules/.bin/nest start --watch",
    "start:debug": "nest start --debug --watch",
    "start:prod": "node dist/main",
    "build": "rimraf dist; nest build",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix"
  },
  "dependencies": {
    "@vize/cgi": "^0.1.3",
    "@vize/editor": "^0.1.6",
    "@vize/generator-web": "^0.1.1",
    "@vize/management-ui": "^0.1.1",
    "@vize/publisher-web": "^0.1.1"
  },
  "devDependencies": {
    "@vize/types": "^0.1.7"
  },
  "author": "",
  "license": "ISC"
}
